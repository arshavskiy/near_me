import {
  EsriUtil,
  FeatureLayerService,
  MapService,
  Service,
  Task
} from "./chunk-D6CRHOBF.js";
import {
  require_leaflet_src
} from "./chunk-WWZU3GNE.js";
import {
  __toESM
} from "./chunk-G3PMV62Z.js";

// node_modules/esri-leaflet-geocoder/package.json
var version = "2.2.14";

// node_modules/esri-leaflet-geocoder/src/Tasks/Geocode.js
var import_leaflet = __toESM(require_leaflet_src());

// node_modules/esri-leaflet-geocoder/src/helper.js
var WorldGeocodingServiceUrl = "https://geocode.arcgis.com/arcgis/rest/services/World/GeocodeServer/";

// node_modules/esri-leaflet-geocoder/src/Tasks/Geocode.js
var Geocode = Task.extend({
  path: "findAddressCandidates",
  params: {
    outSr: 4326,
    forStorage: false,
    outFields: "*",
    maxLocations: 20
  },
  setters: {
    "address": "address",
    "neighborhood": "neighborhood",
    "city": "city",
    "subregion": "subregion",
    "region": "region",
    "postal": "postal",
    "country": "country",
    "text": "singleLine",
    "category": "category",
    "token": "token",
    "key": "magicKey",
    "fields": "outFields",
    "forStorage": "forStorage",
    "maxLocations": "maxLocations",
    // World Geocoding Service (only works with singleLine)
    "countries": "sourceCountry"
  },
  initialize: function(options) {
    options = options || {};
    options.url = options.url || WorldGeocodingServiceUrl;
    Task.prototype.initialize.call(this, options);
  },
  within: function(bounds) {
    bounds = (0, import_leaflet.latLngBounds)(bounds);
    this.params.searchExtent = EsriUtil.boundsToExtent(bounds);
    return this;
  },
  nearby: function(coords, radius) {
    var centroid = (0, import_leaflet.latLng)(coords);
    this.params.location = centroid.lng + "," + centroid.lat;
    this.params.distance = Math.min(Math.max(radius, 2e3), 5e4);
    return this;
  },
  run: function(callback, context) {
    if (this.options.customParam) {
      this.params[this.options.customParam] = this.params.singleLine;
      delete this.params.singleLine;
    }
    return this.request(function(error, response) {
      var processor = this._processGeocoderResponse;
      var results = !error ? processor(response) : void 0;
      callback.call(context, error, { results }, response);
    }, this);
  },
  _processGeocoderResponse: function(response) {
    var results = [];
    for (var i = 0; i < response.candidates.length; i++) {
      var candidate = response.candidates[i];
      if (candidate.extent) {
        var bounds = EsriUtil.extentToBounds(candidate.extent);
      }
      results.push({
        text: candidate.address,
        bounds,
        score: candidate.score,
        latlng: (0, import_leaflet.latLng)(candidate.location.y, candidate.location.x),
        properties: candidate.attributes
      });
    }
    return results;
  }
});
function geocode(options) {
  return new Geocode(options);
}
var Geocode_default = geocode;

// node_modules/esri-leaflet-geocoder/src/Tasks/ReverseGeocode.js
var import_leaflet2 = __toESM(require_leaflet_src());
var ReverseGeocode = Task.extend({
  path: "reverseGeocode",
  params: {
    outSR: 4326,
    returnIntersection: false
  },
  setters: {
    "distance": "distance",
    "language": "langCode",
    "intersection": "returnIntersection"
  },
  initialize: function(options) {
    options = options || {};
    options.url = options.url || WorldGeocodingServiceUrl;
    Task.prototype.initialize.call(this, options);
  },
  latlng: function(coords) {
    var centroid = (0, import_leaflet2.latLng)(coords);
    this.params.location = centroid.lng + "," + centroid.lat;
    return this;
  },
  run: function(callback, context) {
    return this.request(function(error, response) {
      var result;
      if (!error) {
        result = {
          latlng: (0, import_leaflet2.latLng)(response.location.y, response.location.x),
          address: response.address
        };
      } else {
        result = void 0;
      }
      callback.call(context, error, result, response);
    }, this);
  }
});
function reverseGeocode(options) {
  return new ReverseGeocode(options);
}
var ReverseGeocode_default = reverseGeocode;

// node_modules/esri-leaflet-geocoder/src/Tasks/Suggest.js
var import_leaflet3 = __toESM(require_leaflet_src());
var Suggest = Task.extend({
  path: "suggest",
  params: {},
  setters: {
    text: "text",
    category: "category",
    countries: "countryCode",
    maxSuggestions: "maxSuggestions"
  },
  initialize: function(options) {
    options = options || {};
    if (!options.url) {
      options.url = WorldGeocodingServiceUrl;
      options.supportsSuggest = true;
    }
    Task.prototype.initialize.call(this, options);
  },
  within: function(bounds) {
    bounds = (0, import_leaflet3.latLngBounds)(bounds);
    bounds = bounds.pad(0.5);
    var center = bounds.getCenter();
    var ne = bounds.getNorthWest();
    this.params.location = center.lng + "," + center.lat;
    this.params.distance = Math.min(Math.max(center.distanceTo(ne), 2e3), 5e4);
    this.params.searchExtent = EsriUtil.boundsToExtent(bounds);
    return this;
  },
  nearby: function(coords, radius) {
    var centroid = (0, import_leaflet3.latLng)(coords);
    this.params.location = centroid.lng + "," + centroid.lat;
    this.params.distance = Math.min(Math.max(radius, 2e3), 5e4);
    return this;
  },
  run: function(callback, context) {
    if (this.options.supportsSuggest) {
      return this.request(function(error, response) {
        callback.call(context, error, response, response);
      }, this);
    } else {
      console.warn("this geocoding service does not support asking for suggestions");
    }
  }
});
function suggest(options) {
  return new Suggest(options);
}
var Suggest_default = suggest;

// node_modules/esri-leaflet-geocoder/src/Services/Geocode.js
var GeocodeService = Service.extend({
  initialize: function(options) {
    options = options || {};
    if (options.url) {
      Service.prototype.initialize.call(this, options);
      this._confirmSuggestSupport();
    } else {
      options.url = WorldGeocodingServiceUrl;
      options.supportsSuggest = true;
      Service.prototype.initialize.call(this, options);
    }
  },
  geocode: function() {
    return Geocode_default(this);
  },
  reverse: function() {
    return ReverseGeocode_default(this);
  },
  suggest: function() {
    return Suggest_default(this);
  },
  _confirmSuggestSupport: function() {
    this.metadata(function(error, response) {
      if (error) {
        return;
      }
      if (!response.capabilities) {
        this.options.supportsSuggest = false;
      } else if (response.capabilities.indexOf("Suggest") > -1) {
        this.options.supportsSuggest = true;
      } else {
        this.options.supportsSuggest = false;
      }
      this.options.customParam = response.singleLineAddressField.name;
    }, this);
  }
});
function geocodeService(options) {
  return new GeocodeService(options);
}

// node_modules/esri-leaflet-geocoder/src/Controls/Geosearch.js
var import_leaflet5 = __toESM(require_leaflet_src());

// node_modules/esri-leaflet-geocoder/src/Classes/GeosearchCore.js
var import_leaflet4 = __toESM(require_leaflet_src());
var GeosearchCore = import_leaflet4.Evented.extend({
  options: {
    zoomToResult: true,
    useMapBounds: 12,
    searchBounds: null
  },
  initialize: function(control, options) {
    import_leaflet4.Util.setOptions(this, options);
    this._control = control;
    if (!options || !options.providers || !options.providers.length) {
      throw new Error("You must specify at least one provider");
    }
    this._providers = options.providers;
  },
  _geocode: function(text, key, provider) {
    var activeRequests = 0;
    var allResults = [];
    var bounds;
    var callback = import_leaflet4.Util.bind(function(error, results) {
      activeRequests--;
      if (error) {
        return;
      }
      if (results) {
        allResults = allResults.concat(results);
      }
      if (activeRequests <= 0) {
        bounds = this._boundsFromResults(allResults);
        this.fire("results", {
          results: allResults,
          bounds,
          latlng: bounds ? bounds.getCenter() : void 0,
          text
        }, true);
        if (this.options.zoomToResult && bounds) {
          this._control._map.fitBounds(bounds);
        }
        this.fire("load");
      }
    }, this);
    if (key) {
      activeRequests++;
      provider.results(text, key, this._searchBounds(), callback);
    } else {
      for (var i = 0; i < this._providers.length; i++) {
        activeRequests++;
        this._providers[i].results(text, key, this._searchBounds(), callback);
      }
    }
  },
  _suggest: function(text) {
    var activeRequests = this._providers.length;
    var createCallback = import_leaflet4.Util.bind(function(text2, provider2) {
      return import_leaflet4.Util.bind(function(error, suggestions) {
        if (error) {
          return;
        }
        var i2;
        activeRequests = activeRequests - 1;
        if (text2.length < 2) {
          this._suggestions.innerHTML = "";
          this._suggestions.style.display = "none";
          return;
        }
        if (suggestions.length) {
          for (i2 = 0; i2 < suggestions.length; i2++) {
            suggestions[i2].provider = provider2;
          }
        } else {
          this._control._renderSuggestions(suggestions);
        }
        if (provider2._lastRender !== text2 && provider2.nodes) {
          for (i2 = 0; i2 < provider2.nodes.length; i2++) {
            if (provider2.nodes[i2].parentElement) {
              this._control._suggestions.removeChild(provider2.nodes[i2]);
            }
          }
          provider2.nodes = [];
        }
        if (suggestions.length && this._control._input.value === text2) {
          this._control.clearSuggestions(provider2.nodes);
          provider2._lastRender = text2;
          provider2.nodes = this._control._renderSuggestions(suggestions);
          this._control._nodes = [];
        }
      }, this);
    }, this);
    this._pendingSuggestions = [];
    for (var i = 0; i < this._providers.length; i++) {
      var provider = this._providers[i];
      var request = provider.suggestions(text, this._searchBounds(), createCallback(text, provider));
      this._pendingSuggestions.push(request);
    }
  },
  _searchBounds: function() {
    if (this.options.searchBounds !== null) {
      return this.options.searchBounds;
    }
    if (this.options.useMapBounds === false) {
      return null;
    }
    if (this.options.useMapBounds === true) {
      return this._control._map.getBounds();
    }
    if (this.options.useMapBounds <= this._control._map.getZoom()) {
      return this._control._map.getBounds();
    }
    return null;
  },
  _boundsFromResults: function(results) {
    if (!results.length) {
      return;
    }
    var nullIsland = (0, import_leaflet4.latLngBounds)([0, 0], [0, 0]);
    var resultBounds = [];
    var resultLatlngs = [];
    for (var i = results.length - 1; i >= 0; i--) {
      var result = results[i];
      resultLatlngs.push(result.latlng);
      if (result.bounds && result.bounds.isValid() && !result.bounds.equals(nullIsland)) {
        resultBounds.push(result.bounds);
      }
    }
    var bounds = (0, import_leaflet4.latLngBounds)(resultLatlngs);
    for (var j = 0; j < resultBounds.length; j++) {
      bounds.extend(resultBounds[j]);
    }
    return bounds;
  },
  _getAttribution: function() {
    var attribs = [];
    var providers = this._providers;
    for (var i = 0; i < providers.length; i++) {
      if (providers[i].options.attribution) {
        attribs.push(providers[i].options.attribution);
      }
    }
    return attribs.join(", ");
  }
});
function geosearchCore(control, options) {
  return new GeosearchCore(control, options);
}

// node_modules/esri-leaflet-geocoder/src/Providers/ArcgisOnlineGeocoder.js
var ArcgisOnlineProvider = GeocodeService.extend({
  options: {
    label: "Places and Addresses",
    maxResults: 5
  },
  suggestions: function(text, bounds, callback) {
    var request = this.suggest().text(text);
    if (bounds) {
      request.within(bounds);
    }
    if (this.options.countries) {
      request.countries(this.options.countries);
    }
    if (this.options.categories) {
      request.category(this.options.categories);
    }
    request.maxSuggestions(this.options.maxResults);
    return request.run(function(error, results, response) {
      var suggestions = [];
      if (!error) {
        while (response.suggestions.length && suggestions.length <= this.options.maxResults - 1) {
          var suggestion = response.suggestions.shift();
          if (!suggestion.isCollection) {
            suggestions.push({
              text: suggestion.text,
              unformattedText: suggestion.text,
              magicKey: suggestion.magicKey
            });
          }
        }
      }
      callback(error, suggestions);
    }, this);
  },
  results: function(text, key, bounds, callback) {
    var request = this.geocode().text(text);
    if (key) {
      request.key(key);
    }
    request.maxLocations(this.options.maxResults);
    if (bounds) {
      request.within(bounds);
    }
    if (this.options.forStorage) {
      request.forStorage(true);
    }
    if (this.options.countries) {
      request.countries(this.options.countries);
    }
    if (this.options.categories) {
      request.category(this.options.categories);
    }
    return request.run(function(error, response) {
      callback(error, response.results);
    }, this);
  }
});
function arcgisOnlineProvider(options) {
  return new ArcgisOnlineProvider(options);
}

// node_modules/esri-leaflet-geocoder/src/Controls/Geosearch.js
var Geosearch = import_leaflet5.Control.extend({
  includes: import_leaflet5.Evented.prototype,
  options: {
    position: "topleft",
    collapseAfterResult: true,
    expanded: false,
    allowMultipleResults: true,
    placeholder: "Search for places or addresses",
    title: "Location Search"
  },
  initialize: function(options) {
    import_leaflet5.Util.setOptions(this, options);
    if (!options || !options.providers || !options.providers.length) {
      if (!options) {
        options = {};
      }
      options.providers = [arcgisOnlineProvider()];
    }
    this._geosearchCore = geosearchCore(this, options);
    this._geosearchCore._providers = options.providers;
    this._geosearchCore.addEventParent(this);
    for (var i = 0; i < this._geosearchCore._providers.length; i++) {
      this._geosearchCore._providers[i].addEventParent(this);
    }
    this._geosearchCore._pendingSuggestions = [];
    import_leaflet5.Control.prototype.initialize.call(options);
  },
  _renderSuggestions: function(suggestions) {
    var currentGroup;
    if (suggestions.length > 0) {
      this._suggestions.style.display = "block";
    }
    this._suggestions.style.maxHeight = this._map.getSize().y - this._suggestions.offsetTop - this._wrapper.offsetTop - 10 + "px";
    var nodes = [];
    var list;
    var header;
    var suggestionTextArray = [];
    for (var i = 0; i < suggestions.length; i++) {
      var suggestion = suggestions[i];
      if (!header && this._geosearchCore._providers.length > 1 && currentGroup !== suggestion.provider.options.label) {
        header = import_leaflet5.DomUtil.create("span", "geocoder-control-header", this._suggestions);
        header.textContent = suggestion.provider.options.label;
        header.innerText = suggestion.provider.options.label;
        currentGroup = suggestion.provider.options.label;
        nodes.push(header);
      }
      if (!list) {
        list = import_leaflet5.DomUtil.create("ul", "geocoder-control-list", this._suggestions);
      }
      if (suggestionTextArray.indexOf(suggestion.text) === -1) {
        var suggestionItem = import_leaflet5.DomUtil.create("li", "geocoder-control-suggestion", list);
        suggestionItem.innerHTML = suggestion.text;
        suggestionItem.provider = suggestion.provider;
        suggestionItem["data-magic-key"] = suggestion.magicKey;
        suggestionItem.unformattedText = suggestion.unformattedText;
      } else {
        for (var j = 0; j < list.childNodes.length; j++) {
          if (list.childNodes[j].innerHTML === suggestion.text) {
            list.childNodes[j]["data-magic-key"] += "," + suggestion.magicKey;
          }
        }
      }
      suggestionTextArray.push(suggestion.text);
    }
    import_leaflet5.DomUtil.removeClass(this._input, "geocoder-control-loading");
    nodes.push(list);
    return nodes;
  },
  _boundsFromResults: function(results) {
    if (!results.length) {
      return;
    }
    var nullIsland = (0, import_leaflet5.latLngBounds)([0, 0], [0, 0]);
    var resultBounds = [];
    var resultLatlngs = [];
    for (var i = results.length - 1; i >= 0; i--) {
      var result = results[i];
      resultLatlngs.push(result.latlng);
      if (result.bounds && result.bounds.isValid() && !result.bounds.equals(nullIsland)) {
        resultBounds.push(result.bounds);
      }
    }
    var bounds = (0, import_leaflet5.latLngBounds)(resultLatlngs);
    for (var j = 0; j < resultBounds.length; j++) {
      bounds.extend(resultBounds[j]);
    }
    return bounds;
  },
  clear: function() {
    this._suggestions.innerHTML = "";
    this._suggestions.style.display = "none";
    if (this.options.collapseAfterResult) {
      this._input.value = "";
      this._input.placeholder = "";
      import_leaflet5.DomUtil.removeClass(this._wrapper, "geocoder-control-expanded");
    }
    if (!this._map.scrollWheelZoom.enabled() && this._map.options.scrollWheelZoom) {
      this._map.scrollWheelZoom.enable();
    }
  },
  clearSuggestions: function() {
    if (this._nodes) {
      for (var k = 0; k < this._nodes.length; k++) {
        if (this._nodes[k].parentElement) {
          this._suggestions.removeChild(this._nodes[k]);
        }
      }
    }
  },
  _setupClick: function() {
    import_leaflet5.DomUtil.addClass(this._wrapper, "geocoder-control-expanded");
    this._input.focus();
  },
  disable: function() {
    this._input.disabled = true;
    import_leaflet5.DomUtil.addClass(this._input, "geocoder-control-input-disabled");
    import_leaflet5.DomEvent.removeListener(this._wrapper, "click", this._setupClick, this);
  },
  enable: function() {
    this._input.disabled = false;
    import_leaflet5.DomUtil.removeClass(this._input, "geocoder-control-input-disabled");
    import_leaflet5.DomEvent.addListener(this._wrapper, "click", this._setupClick, this);
  },
  getAttribution: function() {
    var attribs = [];
    for (var i = 0; i < this._providers.length; i++) {
      if (this._providers[i].options.attribution) {
        attribs.push(this._providers[i].options.attribution);
      }
    }
    return attribs.join(", ");
  },
  geocodeSuggestion: function(e) {
    var suggestionItem = e.target || e.srcElement;
    if (suggestionItem.classList.length < 1) {
      suggestionItem = suggestionItem.parentNode;
    }
    this._geosearchCore._geocode(suggestionItem.unformattedText, suggestionItem["data-magic-key"], suggestionItem.provider);
    this.clear();
  },
  onAdd: function(map) {
    EsriUtil.setEsriAttribution(map);
    this._map = map;
    this._wrapper = import_leaflet5.DomUtil.create("div", "geocoder-control");
    this._input = import_leaflet5.DomUtil.create("input", "geocoder-control-input leaflet-bar", this._wrapper);
    this._input.title = this.options.title;
    if (this.options.expanded) {
      import_leaflet5.DomUtil.addClass(this._wrapper, "geocoder-control-expanded");
      this._input.placeholder = this.options.placeholder;
    }
    this._suggestions = import_leaflet5.DomUtil.create("div", "geocoder-control-suggestions leaflet-bar", this._wrapper);
    var credits = this._geosearchCore._getAttribution();
    if (map.attributionControl) {
      map.attributionControl.addAttribution(credits);
    }
    import_leaflet5.DomEvent.addListener(this._input, "focus", function(e) {
      this._input.placeholder = this.options.placeholder;
      import_leaflet5.DomUtil.addClass(this._wrapper, "geocoder-control-expanded");
    }, this);
    import_leaflet5.DomEvent.addListener(this._wrapper, "click", this._setupClick, this);
    import_leaflet5.DomEvent.addListener(this._suggestions, "mousedown", this.geocodeSuggestion, this);
    import_leaflet5.DomEvent.addListener(this._input, "blur", function(e) {
      this.clear();
    }, this);
    import_leaflet5.DomEvent.addListener(this._input, "keydown", function(e) {
      var text = (e.target || e.srcElement).value;
      import_leaflet5.DomUtil.addClass(this._wrapper, "geocoder-control-expanded");
      var list = this._suggestions.querySelectorAll(".geocoder-control-suggestion");
      var selected = this._suggestions.querySelectorAll(".geocoder-control-selected")[0];
      var selectedPosition;
      for (var i = 0; i < list.length; i++) {
        if (list[i] === selected) {
          selectedPosition = i;
          break;
        }
      }
      switch (e.keyCode) {
        case 13:
          if (selected) {
            this._input.value = selected.innerText;
            this._geosearchCore._geocode(selected.unformattedText, selected["data-magic-key"], selected.provider);
            this.clear();
          } else if (this.options.allowMultipleResults && text.length >= 2) {
            this._geosearchCore._geocode(this._input.value, void 0);
            this.clear();
          } else {
            if (list.length === 1) {
              import_leaflet5.DomUtil.addClass(list[0], "geocoder-control-selected");
              this._geosearchCore._geocode(list[0].innerHTML, list[0]["data-magic-key"], list[0].provider);
            } else {
              this.clear();
              this._input.blur();
            }
          }
          import_leaflet5.DomEvent.preventDefault(e);
          break;
        case 38:
          if (selected) {
            import_leaflet5.DomUtil.removeClass(selected, "geocoder-control-selected");
          }
          var previousItem = list[selectedPosition - 1];
          if (selected && previousItem) {
            import_leaflet5.DomUtil.addClass(previousItem, "geocoder-control-selected");
          } else {
            import_leaflet5.DomUtil.addClass(list[list.length - 1], "geocoder-control-selected");
          }
          import_leaflet5.DomEvent.preventDefault(e);
          break;
        case 40:
          if (selected) {
            import_leaflet5.DomUtil.removeClass(selected, "geocoder-control-selected");
          }
          var nextItem = list[selectedPosition + 1];
          if (selected && nextItem) {
            import_leaflet5.DomUtil.addClass(nextItem, "geocoder-control-selected");
          } else {
            import_leaflet5.DomUtil.addClass(list[0], "geocoder-control-selected");
          }
          import_leaflet5.DomEvent.preventDefault(e);
          break;
        default:
          for (var x = 0; x < this._geosearchCore._pendingSuggestions.length; x++) {
            var request = this._geosearchCore._pendingSuggestions[x];
            if (request && request.abort && !request.id) {
              request.abort();
            }
          }
          break;
      }
    }, this);
    import_leaflet5.DomEvent.addListener(this._input, "keyup", import_leaflet5.Util.throttle(function(e) {
      var key = e.which || e.keyCode;
      var text = (e.target || e.srcElement).value;
      if (text.length < 2) {
        this._suggestions.innerHTML = "";
        this._suggestions.style.display = "none";
        import_leaflet5.DomUtil.removeClass(this._input, "geocoder-control-loading");
        return;
      }
      if (key === 27) {
        this._suggestions.innerHTML = "";
        this._suggestions.style.display = "none";
        return;
      }
      if (key !== 13 && key !== 38 && key !== 40) {
        if (this._input.value !== this._lastValue) {
          this._lastValue = this._input.value;
          import_leaflet5.DomUtil.addClass(this._input, "geocoder-control-loading");
          this._geosearchCore._suggest(text);
        }
      }
    }, 50, this), this);
    import_leaflet5.DomEvent.disableClickPropagation(this._wrapper);
    import_leaflet5.DomEvent.addListener(this._suggestions, "mouseover", function(e) {
      if (map.scrollWheelZoom.enabled() && map.options.scrollWheelZoom) {
        map.scrollWheelZoom.disable();
      }
    });
    import_leaflet5.DomEvent.addListener(this._suggestions, "mouseout", function(e) {
      if (!map.scrollWheelZoom.enabled() && map.options.scrollWheelZoom) {
        map.scrollWheelZoom.enable();
      }
    });
    this._geosearchCore.on("load", function(e) {
      import_leaflet5.DomUtil.removeClass(this._input, "geocoder-control-loading");
      this.clear();
      this._input.blur();
    }, this);
    return this._wrapper;
  }
});
function geosearch(options) {
  return new Geosearch(options);
}

// node_modules/esri-leaflet-geocoder/src/Providers/FeatureLayer.js
var import_leaflet6 = __toESM(require_leaflet_src());
var FeatureLayerProvider = FeatureLayerService.extend({
  options: {
    label: "Feature Layer",
    maxResults: 5,
    bufferRadius: 1e3,
    formatSuggestion: function(feature) {
      return feature.properties[this.options.searchFields[0]];
    }
  },
  initialize: function(options) {
    FeatureLayerService.prototype.initialize.call(this, options);
    if (typeof this.options.searchFields === "string") {
      this.options.searchFields = [this.options.searchFields];
    }
    this._suggestionsQuery = this.query();
    this._resultsQuery = this.query();
  },
  suggestions: function(text, bounds, callback) {
    var query = this._suggestionsQuery.where(this._buildQuery(text)).returnGeometry(false);
    if (bounds) {
      query.intersects(bounds);
    }
    if (this.options.idField) {
      query.fields([this.options.idField].concat(this.options.searchFields));
    }
    var request = query.run(function(error, results, raw) {
      if (error) {
        callback(error, []);
      } else {
        this.options.idField = raw.objectIdFieldName;
        var suggestions = [];
        for (var i = results.features.length - 1; i >= 0; i--) {
          var feature = results.features[i];
          suggestions.push({
            text: this.options.formatSuggestion.call(this, feature),
            unformattedText: feature.properties[this.options.searchFields[0]],
            magicKey: feature.id
          });
        }
        callback(error, suggestions.slice(0, this.options.maxResults));
      }
    }, this);
    return request;
  },
  results: function(text, key, bounds, callback) {
    var query = this._resultsQuery;
    if (key) {
      delete query.params.where;
      query.featureIds([key]);
    } else {
      query.where(this._buildQuery(text));
    }
    if (bounds) {
      query.within(bounds);
    }
    return query.run(import_leaflet6.Util.bind(function(error, features) {
      var results = [];
      for (var i = 0; i < features.features.length; i++) {
        var feature = features.features[i];
        if (feature) {
          var bounds2 = this._featureBounds(feature);
          var result = {
            latlng: bounds2.getCenter(),
            bounds: bounds2,
            text: this.options.formatSuggestion.call(this, feature),
            properties: feature.properties,
            geojson: feature
          };
          results.push(result);
          delete this._resultsQuery.params["objectIds"];
        }
      }
      callback(error, results);
    }, this));
  },
  orderBy: function(fieldName, order) {
    this._suggestionsQuery.orderBy(fieldName, order);
  },
  _buildQuery: function(text) {
    var queryString = [];
    for (var i = this.options.searchFields.length - 1; i >= 0; i--) {
      var field = 'upper("' + this.options.searchFields[i] + '")';
      queryString.push(field + " LIKE upper('%" + text + "%')");
    }
    if (this.options.where) {
      return this.options.where + " AND (" + queryString.join(" OR ") + ")";
    } else {
      return queryString.join(" OR ");
    }
  },
  _featureBounds: function(feature) {
    var geojson = (0, import_leaflet6.geoJson)(feature);
    if (feature.geometry.type === "Point") {
      var center = geojson.getBounds().getCenter();
      var lngRadius = this.options.bufferRadius / 40075017 * 360 / Math.cos(180 / Math.PI * center.lat);
      var latRadius = this.options.bufferRadius / 40075017 * 360;
      return (0, import_leaflet6.latLngBounds)([center.lat - latRadius, center.lng - lngRadius], [center.lat + latRadius, center.lng + lngRadius]);
    } else {
      return geojson.getBounds();
    }
  }
});
function featureLayerProvider(options) {
  return new FeatureLayerProvider(options);
}

// node_modules/esri-leaflet-geocoder/src/Providers/MapService.js
var import_leaflet7 = __toESM(require_leaflet_src());
var MapServiceProvider = MapService.extend({
  options: {
    layers: [0],
    label: "Map Service",
    bufferRadius: 1e3,
    maxResults: 5,
    formatSuggestion: function(feature) {
      return feature.properties[feature.displayFieldName] + " <small>" + feature.layerName + "</small>";
    }
  },
  initialize: function(options) {
    MapService.prototype.initialize.call(this, options);
    this._getIdFields();
  },
  suggestions: function(text, bounds, callback) {
    var request = this.find().text(text).fields(this.options.searchFields).returnGeometry(false).layers(this.options.layers);
    return request.run(function(error, results, raw) {
      var suggestions = [];
      if (!error) {
        var count = Math.min(this.options.maxResults, results.features.length);
        raw.results = raw.results.reverse();
        for (var i = 0; i < count; i++) {
          var feature = results.features[i];
          var result = raw.results[i];
          var layer = result.layerId;
          var idField = this._idFields[layer];
          feature.layerId = layer;
          feature.layerName = this._layerNames[layer];
          feature.displayFieldName = this._displayFields[layer];
          if (idField) {
            suggestions.push({
              text: this.options.formatSuggestion.call(this, feature),
              unformattedText: feature.properties[feature.displayFieldName],
              magicKey: result.attributes[idField] + ":" + layer
            });
          }
        }
      }
      callback(error, suggestions.reverse());
    }, this);
  },
  results: function(text, key, bounds, callback) {
    var results = [];
    var request;
    if (key) {
      var featureId = key.split(":")[0];
      var layer = key.split(":")[1];
      request = this.query().layer(layer).featureIds(featureId);
    } else {
      request = this.find().text(text).fields(this.options.searchFields).layers(this.options.layers);
    }
    return request.run(function(error, features, response) {
      if (!error) {
        if (response.results) {
          response.results = response.results.reverse();
        }
        for (var i = 0; i < features.features.length; i++) {
          var feature = features.features[i];
          layer = layer || response.results[i].layerId;
          if (feature && layer !== void 0) {
            var bounds2 = this._featureBounds(feature);
            feature.layerId = layer;
            feature.layerName = this._layerNames[layer];
            feature.displayFieldName = this._displayFields[layer];
            var result = {
              latlng: bounds2.getCenter(),
              bounds: bounds2,
              text: this.options.formatSuggestion.call(this, feature),
              properties: feature.properties,
              geojson: feature
            };
            results.push(result);
          }
        }
      }
      callback(error, results.reverse());
    }, this);
  },
  _featureBounds: function(feature) {
    var geojson = (0, import_leaflet7.geoJson)(feature);
    if (feature.geometry.type === "Point") {
      var center = geojson.getBounds().getCenter();
      var lngRadius = this.options.bufferRadius / 40075017 * 360 / Math.cos(180 / Math.PI * center.lat);
      var latRadius = this.options.bufferRadius / 40075017 * 360;
      return (0, import_leaflet7.latLngBounds)([center.lat - latRadius, center.lng - lngRadius], [center.lat + latRadius, center.lng + lngRadius]);
    } else {
      return geojson.getBounds();
    }
  },
  _layerMetadataCallback: function(layerid) {
    return import_leaflet7.Util.bind(function(error, metadata) {
      if (error) {
        return;
      }
      this._displayFields[layerid] = metadata.displayField;
      this._layerNames[layerid] = metadata.name;
      for (var i = 0; i < metadata.fields.length; i++) {
        var field = metadata.fields[i];
        if (field.type === "esriFieldTypeOID") {
          this._idFields[layerid] = field.name;
          break;
        }
      }
    }, this);
  },
  _getIdFields: function() {
    this._idFields = {};
    this._displayFields = {};
    this._layerNames = {};
    for (var i = 0; i < this.options.layers.length; i++) {
      var layer = this.options.layers[i];
      this.get(layer, {}, this._layerMetadataCallback(layer));
    }
  }
});
function mapServiceProvider(options) {
  return new MapServiceProvider(options);
}

// node_modules/esri-leaflet-geocoder/src/Providers/GeocodeService.js
var GeocodeServiceProvider = GeocodeService.extend({
  options: {
    label: "Geocode Server",
    maxResults: 5
  },
  suggestions: function(text, bounds, callback) {
    if (this.options.supportsSuggest) {
      var request = this.suggest().text(text);
      if (bounds) {
        request.within(bounds);
      }
      return request.run(function(error, results, response) {
        var suggestions = [];
        if (!error) {
          while (response.suggestions.length && suggestions.length <= this.options.maxResults - 1) {
            var suggestion = response.suggestions.shift();
            if (!suggestion.isCollection) {
              suggestions.push({
                text: suggestion.text,
                unformattedText: suggestion.text,
                magicKey: suggestion.magicKey
              });
            }
          }
        }
        callback(error, suggestions);
      }, this);
    } else {
      callback(void 0, []);
      return false;
    }
  },
  results: function(text, key, bounds, callback) {
    var request = this.geocode().text(text);
    if (key) {
      request.key(key);
    }
    request.maxLocations(this.options.maxResults);
    if (bounds) {
      request.within(bounds);
    }
    return request.run(function(error, response) {
      callback(error, response.results);
    }, this);
  }
});
function geocodeServiceProvider(options) {
  return new GeocodeServiceProvider(options);
}
export {
  ArcgisOnlineProvider,
  FeatureLayerProvider,
  Geocode,
  GeocodeService,
  GeocodeServiceProvider,
  Geosearch,
  GeosearchCore,
  MapServiceProvider,
  ReverseGeocode,
  Suggest,
  version as VERSION,
  WorldGeocodingServiceUrl,
  arcgisOnlineProvider,
  featureLayerProvider,
  geocode,
  geocodeService,
  geocodeServiceProvider,
  geosearch,
  geosearchCore,
  mapServiceProvider,
  reverseGeocode,
  suggest
};
//# sourceMappingURL=esri-leaflet-geocoder.js.map
